import { calcularCBS } from './cbs';import { calcularIBS } from './ibs';import { calcularIS } from './impostoSeletivo';import { calcularSimples } from './simples';import { calcularLucroPresumido } from './lucroPresumido';import { calcularLucroReal } from './lucroReal';import { TRANSICAO } from './transicao';
export function calcular(d){const creditosEntrada=d.compras+d.servicos+d.ativo;const cbs=calcularCBS({...d,creditosEntrada});const ibs=calcularIBS({...d,creditosEntrada});const is=calcularIS(d.itensIS);const simples=calcularSimples({receitaBruta12m:d.receita12m,receitaMes:d.faturamento,atividade:d.atividade,folha12m:d.folha12m});const lp=calcularLucroPresumido({faturamento:d.faturamento,atividade:d.atividade});const lr=calcularLucroReal({faturamento:d.faturamento});const atual=d.regime==='simples'?simples.das:d.regime==='lucro_presumido'?lp.pis+lp.cofins:lr.pis+lr.cofins;const t=TRANSICAO[d.ano]||TRANSICAO[2033];const novo=cbs.cbsLiquido+ibs.ibsLiquido+is,total=atual*t.velho+novo*t.novo;return{cbs,ibs,is,simples,lp,lr,atual,novo,total,diferenca:novo-atual,transicao:t};}
