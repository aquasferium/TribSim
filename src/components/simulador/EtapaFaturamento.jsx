import InputMoeda from '../ui/InputMoeda';
export default function EtapaFaturamento({dados,setDados}){return <div className='grid gap-2'><InputMoeda value={dados.compras} onChange={v=>setDados(s=>({...s,compras:v}))}/><InputMoeda value={dados.servicos} onChange={v=>setDados(s=>({...s,servicos:v}))}/><InputMoeda value={dados.ativo} onChange={v=>setDados(s=>({...s,ativo:v}))}/><input type='range' min='0' max='100' value={dados.percZero} onChange={e=>setDados(s=>({...s,percZero:Number(e.target.value)}))}/><input type='range' min='0' max='100' value={dados.percReducao} onChange={e=>setDados(s=>({...s,percReducao:Number(e.target.value)}))}/></div>}
