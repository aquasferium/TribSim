import { useSimulacao } from '../hooks/useSimulacao';import { useComparativo } from '../hooks/useComparativo';import GraficoComparativo from '../components/charts/GraficoComparativo';
export default function Comparativo(){const {dados,setDados}=useSimulacao();const comp=useComparativo(dados);const best=comp.reduce((a,b)=>a.resultado.total<b.resultado.total?a:b);return <div><table className='w-full text-sm'><thead><tr><th>Regime</th><th>Total</th></tr></thead><tbody>{comp.map(c=><tr key={c.regime} className={c.regime===best.regime?'border border-ouro':''}><td>{c.regime}</td><td>{c.resultado.total.toFixed(2)}</td></tr>)}</tbody></table><button onClick={()=>setDados(s=>({...s,regime:best.regime}))} className='mt-3 rounded bg-ouro px-3 py-2 text-fundo'>Usar estes dados no Simulador</button><GraficoComparativo data={comp.map(c=>({regime:c.regime,total:c.resultado.total}))}/></div>}
